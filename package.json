{
  "name": "discord-67-bot",
  "version": "1.0.0",
  "description": "Discord bot celebrating the 6-7 meme with persistent stats and leaderboards.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test",
    "check": "node --check index.js && node --check interactions.js && node --check db.js && node --check config.js && node --check triggers/normalize.js && node --check triggers/explicit67.js && node --check triggers/sequence67.js && node --check triggers/count67.js && node --check triggers/every67th.js && node --check triggers/syllable67.js && node --check triggers/timestamp67.js && node --check triggers/wordLength67.js && node --check commands/stats.js && node --check commands/leaderboard.js && node --check commands/streak.js && node --check commands/help.js && node --check commands/resolveUsername.js"
  },
  "keywords": [
    "discord",
    "bot",
    "67",
    "meme"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "discord.js": "^14.18.0",
    "dotenv": "^16.4.7"
  },
  "overrides": {
    "undici": "^6.27.0"
  },
  "allowScripts": {
    "better-sqlite3@12.11.1": true
  }
}
