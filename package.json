{
  "name": "discord-67-bot",
  "version": "1.0.0",
  "description": "Discord bot celebrating the 6-7 meme with persistent stats and leaderboards.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "check": "node --check index.js && node --check db.js && node --check config.js && node --check triggers/explicit67.js && node --check triggers/sequence67.js && node --check triggers/count67.js && node --check triggers/every67th.js && node --check commands/stats.js && node --check commands/leaderboard.js && node --check commands/streak.js && node --check commands/help.js"
  },
  "keywords": [
    "discord",
    "bot",
    "67",
    "meme"
  ],
  "license": "MIT",
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "discord.js": "^14.18.0",
    "dotenv": "^16.4.7"
  }
}
